/*let blur = document.querySelectorAll(".blur");
blur.forEach(function (item) {
  paralax1(item);
});
function paralax1(item) {
  window.addEventListener("mousemove", function (e) {
    let x = e.clientX / window.innerWidth;
    let y = e.clientY / window.innerHeight;
    item.style.transform = "translate(-" + x * 25 + "px, -" + y * 25 + "px)";
    item.style.transform = "translate(-" + x * 25 + "px, -" + y * 25 + "px)";
  });
}
let blur2 = document.querySelectorAll(".blur2");
blur2.forEach(function (item) {
  paralax2(item);
});
function paralax2(item) {
  window.addEventListener("mousemove", function (e) {
    let x = e.clientX / window.innerWidth;
    let y = e.clientY / window.innerHeight;
    item.style.transform = "translate(+" + x * 25 + "px, +" + y * 25 + "px)";
    item.style.transform = "translate(+" + x * 25 + "px, +" + y * 25 + "px)";
  });
}*/
